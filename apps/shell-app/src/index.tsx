/**
 * Shell App Entry Point
 * Bootstrap pattern for Module Federation
 * This allows shared modules to be loaded before the app starts
 */
console.log('[Shell App] v1.0.3 - Initializing...');

import('./bootstrap');
