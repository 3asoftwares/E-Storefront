{
  "installCommand": "cd ../.. && yarn install",
  "buildCommand": "cd ../.. && cd packages/types && npx tsup src/index.ts --format cjs,esm --dts --clean && cd ../utils && npx tsup src/index.ts --format cjs,esm --dts --clean && cd ../ui-library && yarn build && cd ../../apps/admin-app && yarn build",
  "outputDirectory": "dist",
  "framework": "vite",
  "devCommand": "yarn dev",
  "regions": ["iad1"],
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
